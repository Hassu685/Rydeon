import React from 'react';
import ContactPage from '../../../components/contact';

export const metadata = {
  title: "Contact",
};

const page = () => {
    return (
        <>
        <ContactPage/>
        </>
    );
}

export default page;