import React from 'react';
import AboutPage from '../../../components/about';

export const metadata = {
  title: "About",
};

const page = () => {
    return (
        <>
            <AboutPage />
        </>
    );
}


export default page;